{
  "name": "maximo-mcp-oslc-query-builder-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "React frontend for the Maximo OSLC query builder — calls MCP tools via the backend proxy, no client-side OSLC logic",
  "type": "module",
  "scripts": {
    "dev": "echo 'Phase 2: not scaffolded yet — see docs/ROADMAP.md'",
    "build": "echo 'Phase 2: not scaffolded yet — see docs/ROADMAP.md'"
  }
}
